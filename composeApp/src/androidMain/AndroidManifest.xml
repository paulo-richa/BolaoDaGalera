<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".BolaoApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">
        <activity
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.App.Starting"
            android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Filtro para o domínio oficial -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="http" android:host="bolaodagalera.app" android:pathPrefix="/invite" />
                <data android:scheme="https" android:host="bolaodagalera.app" android:pathPrefix="/invite" />
                <data android:scheme="http" android:host="www.bolaodagalera.app" android:pathPrefix="/invite" />
                <data android:scheme="https" android:host="www.bolaodagalera.app" android:pathPrefix="/invite" />
            </intent-filter>

            <!-- Filtro para o domínio Firebase (mais estável para Deep Links) -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="http" android:host="bolaodagalera-bb002.web.app" android:pathPrefix="/invite" />
                <data android:scheme="https" android:host="bolaodagalera-bb002.web.app" android:pathPrefix="/invite" />
            </intent-filter>

            <!-- Filtro Custom Scheme (Garantia de funcionamento) -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="bolaodagalera" android:host="invite" />
            </intent-filter>

            <!-- Deep links usados pelas notificações push (bolão/palpite) -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="bolaodagalera" android:host="bolao" />
                <data android:scheme="bolaodagalera" android:host="predict" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-5592102111451720~6278533072"/>

        <service
            android:name=".notification.BolaoFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>