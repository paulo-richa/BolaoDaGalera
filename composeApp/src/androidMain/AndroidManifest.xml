<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Declared here (not only in :androidApp) because AdBanner.android.kt, which lives in
         this module, calls the AdMob SDK's loadAd() directly - lint flags a library's own
         network calls against its own manifest, independent of what a consuming app declares.
         Harmless duplicate once merged into the final APK alongside :androidApp's manifest. -->
    <uses-permission android:name="android.permission.INTERNET" />

</manifest>
